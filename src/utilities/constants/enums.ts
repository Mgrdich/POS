export enum TableStatus {
    open = 'open',
    closed = 'closed',
    suspended = 'suspended',
    reserved = 'reserved'
}

export enum messageAlert {
    success = 'success',
    error = 'error',
    warning = 'warning',
    info = 'info',
}